services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: espectro
      POSTGRES_PASSWORD: espectro
      POSTGRES_DB: espectro
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U espectro"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  db-data:
